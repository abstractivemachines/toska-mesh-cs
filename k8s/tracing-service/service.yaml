apiVersion: v1
kind: Service
metadata:
  name: toskamesh-tracing-service
  labels:
    app: toskamesh-tracing-service
    component: business
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: toskamesh-tracing-service
