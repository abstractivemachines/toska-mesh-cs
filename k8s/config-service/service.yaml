apiVersion: v1
kind: Service
metadata:
  name: toskamesh-config-service
  labels:
    app: toskamesh-config-service
    component: business
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: toskamesh-config-service
