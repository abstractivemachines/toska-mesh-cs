{{- if and .Values.healthMonitor.enabled .Values.healthMonitor.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "toskamesh.fullname" . }}-health-monitor
  labels:
    {{- include "toskamesh.labels" . | nindent 4 }}
    app.kubernetes.io/component: health-monitor
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "toskamesh.fullname" . }}-health-monitor
  minReplicas: {{ .Values.healthMonitor.hpa.minReplicas }}
  maxReplicas: {{ .Values.healthMonitor.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.healthMonitor.hpa.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.healthMonitor.hpa.targetMemoryUtilizationPercentage }}
{{- end }}
