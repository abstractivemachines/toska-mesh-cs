{{- if and .Values.tracingService.enabled .Values.tracingService.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "toskamesh.fullname" . }}-tracing-service
  labels:
    {{- include "toskamesh.labels" . | nindent 4 }}
    app.kubernetes.io/component: tracing-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "toskamesh.fullname" . }}-tracing-service
  minReplicas: {{ .Values.tracingService.hpa.minReplicas }}
  maxReplicas: {{ .Values.tracingService.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.tracingService.hpa.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.tracingService.hpa.targetMemoryUtilizationPercentage }}
{{- end }}
