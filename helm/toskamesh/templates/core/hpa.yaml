{{- if and .Values.core.enabled .Values.core.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "toskamesh.fullname" . }}-core
  labels:
    {{- include "toskamesh.labels" . | nindent 4 }}
    app.kubernetes.io/component: core
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "toskamesh.fullname" . }}-core
  minReplicas: {{ .Values.core.hpa.minReplicas }}
  maxReplicas: {{ .Values.core.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.core.hpa.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.core.hpa.targetMemoryUtilizationPercentage }}
{{- end }}
