version: '3.8'

services:
  # Infrastructure Services
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: toksa_mesh
      POSTGRES_PASSWORD: toksa_mesh_password
      POSTGRES_DB: toksa_mesh
    ports:
      - "${POSTGRES_PORT:-55432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - toksa-mesh

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6380}:6379"
    networks:
      - toksa-mesh

  consul:
    image: hashicorp/consul:1.16
    ports:
      - "${CONSUL_HTTP_PORT:-18500}:8500"
      - "${CONSUL_DNS_PORT:-18600}:8600"
    command: agent -server -ui -bootstrap-expect=1 -client=0.0.0.0
    networks:
      - toksa-mesh

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "${PROMETHEUS_PORT:-19090}:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus.rules.yml:/etc/prometheus/prometheus.rules.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - toksa-mesh

  grafana:
    image: grafana/grafana:latest
    ports:
      - "${GRAFANA_PORT:-13000}:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    networks:
      - toksa-mesh

  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    ports:
      - "${RABBITMQ_PORT:-5673}:5672"
      - "${RABBITMQ_UI_PORT:-15673}:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - toksa-mesh

  rabbitmq-exporter:
    image: kbudde/rabbitmq-exporter:latest
    ports:
      - "9419:9419"
    environment:
      - RABBIT_URL=http://rabbitmq:15672
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest
    depends_on:
      - rabbitmq
    networks:
      - toksa-mesh

  redis-exporter:
    image: oliver006/redis_exporter:latest
    ports:
      - "9121:9121"
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    networks:
      - toksa-mesh

  # Core Services
  gateway:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.Gateway
    ports:
      - "${GATEWAY_PORT:-15000}:8080"
      - "${GATEWAY_HTTPS_PORT:-15443}:8443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Urls=http://+:8080
      - Consul__Address=http://consul:8500
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Mesh__ServiceDiscovery__Grpc__AllowInsecureTransport=true
      - Jwt__SecretKey=${JWT_SECRET:-local-dev-jwt-secret-32chars}
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Dashboard__PrometheusBaseUrl=http://prometheus:9090
      - Dashboard__TracingBaseUrl=http://tracing-service:8080
      - Dashboard__DiscoveryBaseUrl=http://discovery:8080
      - Dashboard__HealthMonitorBaseUrl=http://health-monitor:8080
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
      - Mesh__Gateway__Tls__ClientCertificatePath=/etc/tls/gateway-client.pfx
      - Mesh__Gateway__Tls__ClientCertificatePassword=${GATEWAY_CLIENT_CERT_PASSWORD:-}
    depends_on:
      - consul
      - discovery
      - prometheus
    networks:
      - toksa-mesh
    volumes:
      - ./certs/gateway:/etc/tls:ro

  discovery:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.Discovery
    ports:
      - "${DISCOVERY_PORT:-15010}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Urls=http://+:8080
      - Messaging__RabbitMqHost=rabbitmq
      - Consul__Address=http://consul:8500
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
    depends_on:
      - consul
      - rabbitmq
    networks:
      - toksa-mesh

  core:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.Core
    ports:
      - "8080:8080"
      - "11111:11111"
      - "30000:30000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Orleans__ServiceId=ToskaMesh
      - Orleans__ClusterId=dev
      - Orleans__ClusterProvider=Consul
      - Orleans__ConsulAddress=http://consul:8500
      - Orleans__AdvertisedIPAddress=${ORLEANS_ADVERTISED_IP:-}
      - Consul__Address=http://consul:8500
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
      - Orleans__DatabaseConnectionString=Host=postgres;Database=toksa_mesh;Username=toksa_mesh;Password=toksa_mesh_password
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
    depends_on:
      - postgres
      - consul
    networks:
      - toksa-mesh

  health-monitor:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.HealthMonitor
    ports:
      - "5005:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Urls=http://+:8080
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Consul__Address=http://consul:8500
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
    depends_on:
      - discovery
      - consul
    networks:
      - toksa-mesh

  # Business Services
  auth-service:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.AuthService
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=toksa_mesh;Username=toksa_mesh;Password=toksa_mesh_password
      - Database__AutoMigrate=true
      - Consul__Address=http://consul:8500
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
    depends_on:
      - postgres
      - consul
      - discovery
    networks:
      - toksa-mesh

  config-service:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.ConfigService
    ports:
      - "5002:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=toksa_mesh;Username=toksa_mesh;Password=toksa_mesh_password
      - Database__AutoMigrate=true
      - Consul__Address=http://consul:8500
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Jwt__Secret=${JWT_SECRET:-local-dev-jwt-secret-32chars}
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
    depends_on:
      - postgres
      - consul
      - discovery
    networks:
      - toksa-mesh

  metrics-service:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.MetricsService
    ports:
      - "5003:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=toksa_mesh;Username=toksa_mesh;Password=toksa_mesh_password
      - Database__AutoMigrate=true
      - Consul__Address=http://consul:8500
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Jwt__Secret=${JWT_SECRET:-local-dev-jwt-secret-32chars}
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Mesh__Telemetry__Sampling__Strategy=ParentBasedAlwaysOn
      - Mesh__Telemetry__Sampling__Ratio=1
    depends_on:
      - postgres
      - prometheus
      - consul
      - discovery
    networks:
      - toksa-mesh

  tracing-service:
    build:
      context: ../
      dockerfile: deployments/Dockerfile.TracingService
    ports:
      - "5004:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=toksa_mesh;Username=toksa_mesh;Password=toksa_mesh_password
      - Database__AutoMigrate=true
      - Consul__Address=http://consul:8500
      - Mesh__ServiceDiscovery__Grpc__Address=http://discovery:8080
      - Jwt__Secret=${JWT_SECRET:-local-dev-jwt-secret-32chars}
      - Mesh__ServiceAuth__Secret=${MESH_SERVICE_AUTH_SECRET:-local-dev-mesh-service-secret-32chars}
      - Mesh__ServiceAuth__Issuer=${MESH_SERVICE_AUTH_ISSUER:-ToskaMesh.Services}
      - Mesh__ServiceAuth__Audience=${MESH_SERVICE_AUTH_AUDIENCE:-ToskaMesh.Services}
      - Mesh__Telemetry__TracingIngest__Enabled=true
      - Mesh__Telemetry__TracingIngest__Endpoint=http://tracing-service:8080/api/traces/ingest
      - Tracing__Retention__RetentionDays=14
      - Tracing__Retention__CleanupIntervalMinutes=60
      - Tracing__Retention__BatchSize=20000
    depends_on:
      - postgres
      - consul
      - discovery
    networks:
      - toksa-mesh

networks:
  toksa-mesh:
    driver: bridge

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:
  rabbitmq_data:
